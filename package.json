{
  "name": "trisynaptic",
  "version": "0.0.1",
  "description": "",
  "main": "script.js",
  "scripts": {
    "start": "node build.js",
    "build": "esbuild script.js --outfile=dist/script.js --minify --bundle && cp index.html dist",
    "format": "prettier --write *.{js,html}",
    "lint": "eslint script.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^3.5.2",
    "esbuild": "^0.14.10",
    "prettier": "^2.5.1",
    "servor": "^4.0.2"
  },
  "devDependencies": {
    "eslint": "^8.6.0"
  }
}
