{
  "name": "trisynaptic",
  "version": "0.0.1",
  "description": "",
  "main": "script.js",
  "scripts": {
    "start": "node build.js",
    "build": "esbuild script.js --outdir=dist --minify --bundle && cp index.html dist",
    "format": "prettier --write *.{js,html}"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.1.15",
    "exec-sh": "^0.3.4",
    "prettier": "^2.0.5",
    "stats.js": "^0.17.0"
  }
}
